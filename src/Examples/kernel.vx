# kernel.vx – test GPU kernel compilation and launch stub
# Uses hybrid syntax: colon + indented block + explicit '}'.

@device
dev_val: i32 = 0          # device variable (resides in GPU memory)

@kernel fn add_kernel(a: i32, b: i32, result: &mut i32):
    *result = a + b
}

fn main() -> i32:
    let mut res: i32 = 0
    add_kernel_launch(5, 7, &mut res)   # auto‑generated stub
    if res != 12:
        return 1
    }
    return 0
}
