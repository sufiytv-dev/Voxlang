# kernel.vx – GPU kernel test (debug mode)

@kernel(block=(1,1,1)) fn add_kernel(a: i32, b: i32, result: &mut i32):
    *result = a + b
}

fn main() -> i32:
    let mut res: i32 = 0
    launch add_kernel(1, 1, 1)(5, 7, &mut res)
    if res != 12:
        return 1
    }
    return 0
}
