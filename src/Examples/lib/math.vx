# A simple module exporting an add function.
fn add(a: i32, b: i32) -> i32:
    return a + b
}

fn sub(a: i32, b: i32) -> i32:
    return a - b
}

fn mul(a: i32, b: i32) -> i32:
    return a * b
}

struct Point:
    x: i32
    y: i32
}
